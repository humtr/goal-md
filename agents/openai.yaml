interface:
  display_name: ".Goal-md"
  short_description: "Plan, draft, and lift GOAL.md for long-running work."
  default_prompt: "Use $goal-md to analyze a project, draft GOAL.md, and raise its acceptance threshold."
